version: '3'
services:
  web:
    image: remix-infonomic-io-c6b7:web-1.0.0
    container_name: remix
    environment:
      # DATABASE_URL: ${DATABASE_URL}
      SESSION_SECRET: ${SESSION_SECRET}
    ports:
      - 80:8080
